What is react?
react is free and open-source javascript library spically designed for building user interface.

what is open-source?
open-source refer to any type of software or basically anything where source code is available to the public.

what is library?
library is a collection of code that developer can used to perform a common task or to add specific functionality to their application.

the UI is the means through which the user interact with our application.

developed by facebook in 2011?
react is the most popular, powerful front-end library developed and sponsored by facebook

use components model.
a component is a piece of the ui that has its own logic and appearance. a component can be small as button or large as entire page.

build modular apps.
once learn react, you will not only be able to build web page but can also build the mobile app, dekstop and virtual reality.

learn once, write anywhere

commands in command pads
cd dekstop
cd specific folder.
first write (npm create vite@latest)
then press ok
then project name.
then chose the framework react.
then choose the varient javascript.

o Use Vite 8 beta (Experimental)?:
| No
|
o Install with npm and start now?
| No
you have to write no.

then open the folder and go to the terminal
then write cd projectName like first.
then npm run install. it will create node-modules
then npm run dev . it will give you the output in webpage.

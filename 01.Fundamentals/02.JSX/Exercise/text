<!-- What Is JSX?
JSX allows you to write HTML in react. JSX makes it easier to write and add HTML in react.
JSX mean javascript XML.

important note
1 .  in jsx file in one component there should bu one parent like

const app = ()=>{
    <section className="section">
    here you have to include everything
    
    </section>
    this is the parent and there shoul be one parent and everything should be inside it
}


2. you have to close all parenthesis in html on have save a setting that is format on save but in JSX you have to close it menually otherwise the compiler show an error.

3. in JSX instead of writing class you have to write className like
<h2 className="h2">hellow</h2>
if you write class the console give you a warning that is (invalid DOM property `class` Did you mean className)

4.       <form>
        <label htmlFor="name">name</label>
        <input type="text" placeholder="Enter Name" />
      </form>

      inside form if you define label for name than you will write htmlFor if instead of write htmlFor if you write

            <form>
        <label for="name">name</label>
        <input type="text" placeholder="Enter Name" />
      </form> the javascript compiler will think that we are creating a loop. so give an error message -->
